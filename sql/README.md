SQL Analytics Layer
===================

This folder contains all SQL scripts used to build the analytics layer
for the Olist E-Commerce Analytics Project.

Workflow:
- Data Quality Checks on raw tables
- Staging layer (stg schema)
- Analytics layer (mart schema)
- Fact and dimension tables for BI consumption

PostgreSQL is used as the analytical database.
SQL serves as the single source of truth for Power BI.

